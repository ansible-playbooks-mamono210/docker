---
- name: Create keypair
  hosts: localhost
  connection: local
  gather_facts: false

  tasks:
    - name: Delete remote keypair
      ec2_key:
        name: "{{ keypair_name }}"
        state: absent
