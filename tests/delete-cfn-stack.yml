---
- name: Create keypair
  hosts: localhost
  connection: local
  gather_facts: false

  tasks:
    - name: Create CloudFormation stack
      amazon.aws.cloudformation:
        stack_name: '{{ aws_cfn_stackname }}'
        state: absent
